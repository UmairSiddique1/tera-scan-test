<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TerascanTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.TerascanTest" parent="Base.Theme.TerascanTest" >
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="OnSelectBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <!-- Customize other attributes if needed -->
<!--        <item name="itemIconTint">#A36DFF</item>-->
<!--        <item name="itemTextColor">#A36DFF</item>-->
        <!-- Add other custom attributes as needed -->
    </style>

    <style name="UnSelectBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <!-- Customize other attributes if needed -->
        <item name="itemIconTint">#858585</item>
        <item name="itemTextColor">#858585</item>
        <!-- Add other custom attributes as needed -->
    </style>
</resources>